# src 코드 리뷰 결과

- 리뷰 대상: `src/index.js`, `src/pipeline.js`, `src/config.js`, `src/crawl-site.js`, `src/fetch-sheet.js`, `src/submit-data.js`, `src/transform-data.js`, `src/discord.js`, `src/errors.js`
- 리뷰 기준: 버그/잠재 오류, 보안, 코드 품질, 성능, 유지보수성

## src/index.js
이슈 없음

## src/pipeline.js
- 🔴 치명 | 버그/운영 리스크 | L72, L81-L83, L93-L107 | 사이트 제출 성공 후 Discord 전송 실패 시 전체 파이프라인을 실패로 `throw`합니다. 실제 저장은 완료됐는데 작업 실패로 인식되어 재실행 시 중복 저장 위험이 있습니다. 권장: 제출 단계와 알림 단계를 분리하고, 알림 실패는 경고로만 처리.
- 🟠 주의 | 보안(민감정보 노출) | L98, L29 | 실패 알림에 `error.message`를 그대로 전송합니다. 하위 단계에서 디버그 덤프가 포함되면 개인정보(이름/생년월일 등)가 Discord로 유출될 수 있습니다. 권장: 알림용 메시지 마스킹/요약.
- 🟡 개선 | 유지보수성 | L65 | `runPipeline` 내부에서 `config.google.range`를 직접 변경합니다. 함수 외부에서 동일 `config`를 재사용하면 부작용이 생깁니다. 권장: 로컬 변수(`effectiveRange`) 사용.

## src/config.js
- 🟠 주의 | 버그/예외처리 | L81, L48-L53, L85, L109 | `TIMEZONE` 유효성 검증이 없어 잘못된 값이면 `toLocaleString`/`Intl`에서 `RangeError`가 발생할 수 있습니다. 권장: 로드 시 IANA timezone 검증 후 실패 메시지 명확화.
- 🟡 개선 | 유지보수성 | L3-L10 | `DISCORD_WEBHOOK_URL`이 항상 필수입니다. `DRY_RUN=true`에서도 강제되어 테스트/로컬 실행 유연성이 떨어집니다. 권장: dry-run 시 필수키에서 제외하거나 경고로 다운그레이드.

## src/crawl-site.js
- 🟠 주의 | 버그/리소스 누수 | L32-L36, L44 | 브라우저/컨텍스트/페이지 생성이 `try` 블록 밖에 있어, 초기화 중간 실패 시 정리 코드가 실행되지 않을 수 있습니다. 권장: 생성 단계 포함 전체를 `try/finally`로 감싸기.
- 🟠 주의 | 예외 은닉 | L20-L28 | `clickIfExists`가 모든 예외를 `false`로 흡수합니다. 선택자 문법 오류/페이지 상태 오류까지 동일 처리되어 원인 파악이 어려워집니다. 권장: timeout만 `false` 처리하고 나머지는 재`throw`.
- 🟡 개선 | 성능/안정성 | L49, L55, L78, L92, L100, L107 | 고정 sleep(`waitForTimeout`) 비중이 높아 환경에 따라 불필요 대기 또는 레이스 컨디션이 발생합니다. 권장: DOM 조건 기반 대기로 치환.

## src/fetch-sheet.js
- 🟠 주의 | 데이터 정합성 | L10-L19 | 헤더 행 탐지 실패 시 0행을 헤더로 강제 사용합니다. 잘못된 헤더 매핑이 조용히 진행되어 출결 분류가 틀어질 수 있습니다. 권장: 헤더 미탐지 시 즉시 예외 처리.
- 🟡 개선 | 유지보수성/관측성 | L39-L45, L60 | 실제 조회에 사용한 범위는 `targetRange`인데 반환값은 `config.google.range`입니다. 로그/디버깅 시 실제 조회 범위를 오해할 수 있습니다. 권장: 반환값을 `targetRange`로 통일.

## src/submit-data.js
- 🟠 주의 | 버그/검증 누락 | L50-L57 | 저장 버튼 클릭 후 성공 여부를 검증하지 않고 성공으로 확정합니다. 서버 측 저장 실패/검증 오류가 있어도 false positive가 됩니다. 권장: 성공 토스트/응답 상태/저장 완료 문구 확인 로직 추가.
- 🟡 개선 | 코드 품질 | L1 | `chromium` import가 사용되지 않습니다. 권장: 불필요 import 제거.

## src/transform-data.js
- 🔴 치명 | 버그(오분류/오작동) | L16-L23 | `훈련일자` 미탐지 시 본문 전체에서 첫 날짜를 사용합니다. 본문에는 생년월일(`1996-...`)이 다수 포함되어 잘못된 날짜를 선택할 가능성이 큽니다. 권장: fallback 추론 대신 실패 처리하거나 `훈련일자` 주변 토큰만 제한 검색.
- 🟠 주의 | 버그/엣지 케이스 | L47-L56 | `resultIdx`와 `endIdx` 존재만 검사하고 순서(`resultIdx < endIdx`)는 확인하지 않습니다. 순서가 바뀌면 빈 `studentBlock`으로 조용히 진행됩니다. 권장: 순서 검증 후 예외 처리.
- 🟠 주의 | 데이터 정합성 | L83-L95 | 날짜 컬럼 키 탐색이 첫 레코드 구조에 강하게 의존합니다. 첫 레코드가 비정형이면 전체 매핑이 빈 값으로 떨어질 수 있습니다. 권장: 헤더 기준 고정 매핑 또는 모든 레코드의 키 집합에서 컬럼 탐색.
- 🟠 주의 | 보안(민감정보 노출) | L20, L51 | 예외 메시지에 본문 preview/dump를 포함합니다. 상위에서 그대로 Discord로 전송되어 개인정보 노출 위험이 있습니다. 권장: 디버그 덤프는 로컬 로그로 분리하고 알림 메시지는 마스킹.

## src/discord.js
- 🟠 주의 | 성능/안정성 | L4-L10 | `fetch`에 timeout/abort가 없어 네트워크 장애 시 장시간 대기할 수 있습니다. 권장: `AbortController` 기반 timeout 도입.
- 🟠 주의 | 버그/운영 리스크 | L9 | Discord 메시지 길이(약 2000자) 제한을 사전 검증하지 않습니다. 길이 초과 시 400으로 실패하며, 파이프라인 전체 실패로 이어질 수 있습니다. 권장: 길이 체크 후 분할 전송.

## src/errors.js
이슈 없음
