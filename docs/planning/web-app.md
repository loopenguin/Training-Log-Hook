# 웹 앱 (SPA/MPA) 기획서

> 공통 항목은 [README.md](README.md)를 먼저 작성하세요.
> 아래는 웹 앱 고유 항목입니다.

---

## 1. 페이지 구조

> 가이드: 앱의 전체 화면 목록과 계층을 정의합니다.
> SPA면 라우트 경로, MPA면 HTML 파일 단위로 작성합니다.

| 페이지/라우트 | 설명 | 주요 컴포넌트 | 접근 권한 |
|--------------|------|-------------|----------|
| `/` | 메인 화면 | | 전체 |
| `/login` | 로그인 | | 비인증 |
| | | | |

**네비게이션 흐름:**
```
메인 → (경로 1) → ...
     → (경로 2) → ...
```

---

## 2. 핵심 기능

> 가이드: MVP에 포함될 기능을 우선순위 순으로 나열합니다.
> 각 기능의 입력/처리/출력을 명시하세요.

| # | 기능명 | 설명 | 입력 | 출력 | 우선순위 |
|---|--------|------|------|------|---------|
| F1 | | | | | P0 (필수) |
| F2 | | | | | P0 (필수) |
| F3 | | | | | P1 (권장) |
| F4 | | | | | P2 (향후) |

---

## 3. 데이터 모델

> 가이드: 앱이 다루는 핵심 데이터 엔티티를 정의합니다.
> DB를 쓰지 않더라도 앱 내부 상태 구조를 명시합니다.

### 엔티티 정의

```
엔티티명:
  - 필드1: 타입 (설명)
  - 필드2: 타입 (설명)
```

### 데이터 흐름

```
사용자 입력 → [처리] → 화면 표시
                    → [저장소] (localStorage / API / 없음)
```

---

## 4. API 설계 (해당 시)

> 가이드: 외부 API를 호출하거나, 자체 백엔드가 있는 경우 작성합니다.
> 순수 프론트엔드 앱이면 "해당 없음"으로 표시합니다.

| 엔드포인트 | 메서드 | 요청 | 응답 | 용도 |
|-----------|--------|------|------|------|
| | | | | |

**인증 방식**: (API 키 / OAuth / 없음)

**외부 API 의존성**:
| API | 용도 | 무료 한도 | 키 관리 방식 |
|-----|------|----------|-------------|
| | | | |

---

## 5. UI/UX 요구사항

> 가이드: 디자인 방향을 구체적으로 명시합니다.
> "깔끔한 UI" 같은 추상적 표현 대신 구체적 기준을 적으세요.

**디자인 참조 파일**: (design.md 경로 또는 "없음" — 공통 항목 §3 참조)

| 항목 | 정의 |
|------|------|
| **레이아웃 기조** | (고정 사이드바 / 탭 기반 / 단일 컬럼 등) |
| **색상 팔레트** | 주색: / 보조색: / 배경: (또는 "자동 선택") |
| **타이포그래피** | 본문 크기, 폰트 계열 (또는 "시스템 기본") |
| **반응형** | (필수 / 데스크톱 우선 / 불필요) |
| **다크모드** | (지원 / 미지원) |
| **접근성** | (기본 시맨틱 / WCAG AA / 고려 안 함) |

**참고 UI**: (비슷한 느낌의 기존 앱이나 스크린샷이 있으면 경로 기재)

---

## 6. 빌드/배포

| 항목 | 정의 |
|------|------|
| **번들러** | (Vite / Webpack / 없음(바닐라)) |
| **배포 대상** | (GitHub Pages / Vercel / 로컬 전용) |
| **빌드 산출물** | (단일 HTML / dist 폴더 / Docker) |
| **환경 변수** | (API 키 등 — `.env` 관리 여부) |
